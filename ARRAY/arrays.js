// Structure of an Array.
let shoppingList = [ 'tomatoes','pears','tofu'];
//console.log(shoppingList)// return 'tomatoes', 'pears, 'tofu'
// Array [ list of items] Different type of items.
// Unlike regular object like person that we assigned properties to
// Arrays [ ] automatically receive properties. e.g. shoppingList.length 
console.log(shoppingList.length);
// this will return a 3. Number of items or elements
// You can perform many operations in an array using the .notation