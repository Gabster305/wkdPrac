// Objects are like objects in real life. e.g. a person.
// A person has a name, age, address, phone, email ...etc.
// Instead of assigning all these as variable separately e.g let name = 'Gaby';
// We make this variables properties of the object Person.
// The syntax to an object 
    let person = {
    name: 'Gaby',
    age: 35
};
//console.log(person)  //Here we receive all the properties of that object
 // If we want to receive only certain properties of that object then we use 
 // Dot Notation e.g.
 console.log (person.name) // we only receive Gaby

 // Bracket Notation e.g.
 // let selection = 'name';
 // person[name] = 'Mary';
 //
 // console.log(person.name);



